# OutlookAccessInterface

Reads out calendar data from an outlook file and imports them into an access database. (!Purpous built)

## code style

    member fields are defined as attributes without setters
    setters are defined as methods to avoid confusion

### class structure

    -   //NSEC: class members
        //NSEC: class fields
        //NSEC: class methods
    -   //NSEC: instance members
        //NSEC: instance fields
        //NSEC: instance methods